var text = new createjs.Text(
   "HAPPY BIRTHDAY EYA!!!",
   "bold 24px Arial", "#fff"};
   text.textAlign = "center";
   text.x = w / 2;
   text.y = h / 2 - text.
   getMeasuredLineHeight();
   stage.addChild(text);
